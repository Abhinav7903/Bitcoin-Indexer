services:
  db:
    image: apache/age:latest
    environment:
      POSTGRES_USER: hornet
      POSTGRES_PASSWORD: H0rnSt@r
      POSTGRES_DB: btc_indexer
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
